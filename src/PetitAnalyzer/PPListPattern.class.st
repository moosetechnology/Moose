"
PPListPattern that is used to match any number of parsers. As its superclass, it cannot be used for actually parsing something.
"
Class {
	#name : #PPListPattern,
	#superclass : #PPPattern,
	#category : #'PetitAnalyzer-Core'
}
