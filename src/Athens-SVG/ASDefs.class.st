Class {
	#name : #ASDefs,
	#superclass : #ASContainerElement,
	#category : #'Athens-SVG'
}

{ #category : #rendering }
ASDefs >> renderOn: aCanvas [
	"defs are not rendered directly"
]

{ #category : #rendering }
ASDefs >> renderShapeOn: aCanvas [

	"defs are not rendered directly"
]
