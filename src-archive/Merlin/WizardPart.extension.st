Extension { #name : #WizardPart }

{ #category : #'*Merlin-Rendering' }
WizardPart >> outputValue: anObject [

	"populate the output value of a wizardPart with aObject. Will be used by the rendering package"
	output := anObject
]
